# Real Findings Identified in Sprint 2

This report is generated by:

```bash
python analysis/evaluation/run_quality_assessment.py --write
```

The repository does not include real collected leak documents because collector
outputs are git-ignored. After at least 200 parsed documents exist locally, the
runner will list the top suspicious or high-risk candidates here without copying
raw leak content into the repository.

For demo-only validation, use the synthetic dataset:

```bash
python analysis/evaluation/run_quality_assessment.py --demo --write
```

Synthetic candidates must not be presented as real leak findings.

## Current Local Result

No suspicious or high-risk finding candidates are available in the committed
repository snapshot.

## Review Method

For each candidate, the generated table records:

- Rank
- Classification
- Risk score
- Matched company
- Pattern and terminology signals
- Local source path

The report intentionally avoids storing raw leak text. Analysts should open the
local source path during review and record remediation notes in the backend
review workflow.

## Next Run

After collectors and ingestion produce local documents:

```bash
python analysis/evaluation/run_quality_assessment.py --write
```
